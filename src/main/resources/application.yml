spring:
  config:
    import: optional:application-secret.yml

  datasource:
    url: jdbc:mysql://localhost:3306/evo_style
    username: root
    password: jmgptw13!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: ${kafka.url}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      group-id: delivery-update-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    listener:
      ack-mode: record

delivery:
  api:
    base-url: ${delivery.url}
    uri: ${delivery.uri}

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: debug

server:
  port: 8080

jwt:
  secret:
    key: ${jwt.secret.key}

payment:
  toss:
    test-client-key: test_ck_24xLea5zVAz4kK5v0aY6rQAMYNwW
    test-secret-key: test_sk_yL0qZ4G1VOlGMeM4w2xvroWb2MQY
